# Notebooks

The notebooks are lightweight Colab entry points for installation and smoke testing. Paper-scale experiments are controlled by the YAML configurations and command-line scripts so that the same commands run in Colab, local workstations, and cluster jobs.

- `pns_quickstart.ipynb`
- `lc_quickstart.ipynb`
